\begin{onlycompile}
\begin{code}
module Exer040609 where
\end{code}
\end{onlycompile}

\paragraph{練習問題 4.6.9}
\verb$segs1$を\verb$segs1 = concat . map inits1 . tails1$のように定義せよ．ただし，\verb$inits1$はリストの空ではない先頭部分リストを返し，\verb$tails1$は空ではない末尾部分リストを返すものとする．これを使って，空の部分列を除外した版の最大部分和問題を述べ，それを解け．
\verb||

----
